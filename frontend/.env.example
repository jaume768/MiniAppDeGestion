# URL de la API Django
# Las peticiones las hace el navegador, por lo que siempre usa localhost
NEXT_PUBLIC_API_URL=http://localhost:8000

# Para producción cambiar por la URL del servidor:
# NEXT_PUBLIC_API_URL=https://tu-api.com
